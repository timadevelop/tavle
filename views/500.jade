doctype
head
	title='Ошибка - нет такой страницы'
	link(rel='stylesheet' href='/css/about.min.css')
	link(href='/ico/fav.ico' rel="shortcut icon" type="image/x-icon")
	script(src='/js/jquery-1.11.2.min.js')
	script(src='/js/jquery.scrollTo.min.js')
	script(src='/js/jquery.mb.YTPlayer.min.js')
body
	header(style='min-height: 100%;')
		section.big-background
			a.player( style='min-height: 100%;',data-property="{videoURL:'https://www.youtube.com/watch?v=-lnGYb3fLsA',containment:'body',autoPlay:true, mute:true, startAt:0, opacity:1}")
			.pattern
				.big-background-container
					h1.big-background-title Tavle
					br
					.divider
						h1#colorize Что-то пошло не так :(
						h1#colorize(style='line-height: 1em;') Просим сообщить о проблеме администраторам.
						br
						a.big-background-btn(href='/feedback', style='border-right:none;') Сообщить об ошибке
						a.big-background-btn(href='/', style='border-left:none;') Перейти на главную
	script(type='text/javascript').
		$(document).on('click','.scrollTo', function(){
			var el = $(this).attr('href');
			$.scrollTo(el, 500);
		});
		$(function(){
			var is_mobile = false;
			if( $('.player').css('display')=='none') {
				is_mobile = true;   
			}
			if (is_mobile == true) {
				//Conditional script here
				$('.big-background').addClass('big-background-default-image');
			}else{
				$(".player").mb_YTPlayer(); 
			}
		});
	style.
		.mbYTP_wrapper{
			 min-height: 100% !important;
			 z-index: 500 !important;
		}
